# MongoDB Atlas connection string (Database -> Connect -> Drivers -> Python).
# Replace <db_password> with your database user's password.
MONGODB_URI=mongodb+srv://<username>:<db_password>@<cluster-url>/?retryWrites=true&w=majority

# Database name (created automatically on first write)
MONGODB_DB=bhu_drishti
